def main():
    """Основная функция скрипта"""
    
    print("=== Анализ трат за неделю ===")
    
    # Создаем список трат за 7 дней
    expenses = [450, 320, 780, 150, 920, 210, 650]  # в рублях
    
    # Альтернатива: ввод от пользователя
    # expenses = []
    # for day in range(1, 8):
    #     expense = float(input(f"Введите траты за день {day}: "))
    #     expenses.append(expense)
    
    print(f"\nТраты за неделю: {expenses}")
    
    # Вычисления
    total = sum(expenses)  # Сумма
    average = total / len(expenses)  # Среднее
    minimum = min(expenses)  # Минимум
    maximum = max(expenses)  # Максимум
    
    # Вывод результатов
    print(f"\n📊 Результаты анализа:")
    print(f"• Сумма всех трат: {total:.2f} руб.")
    print(f"• Средняя трата в день: {average:.2f} руб.")
    print(f"• Минимальная трата: {minimum:.2f} руб.")
    print(f"• Максимальная трата: {maximum:.2f} руб.")
    
    # Создаем кортеж
    result_tuple = (minimum, maximum, total)
    
    print(f"\n📦 Кортеж (минимум, максимум, сумма): {result_tuple}")
    print(f"Тип данных: {type(result_tuple)}")
    
    # Дополнительная информация
    print(f"\n🔍 Детализация:")
    for i, expense in enumerate(expenses, 1):
        print(f"  День {i}: {expense} руб. ({expense - average:+.2f} руб. от среднего)")

if __name__ == "__main__":
    main()